prelude: |
  $LOAD_PATH.unshift File.expand_path('lib')
  require 'string_bits'
  DATA = Random.bytes(1_000_000)
  POPCOUNT_TABLE = Array.new(256) { |i| i.to_s(2).count("1") }.freeze

benchmark:
  baseline: DATA.each_byte.sum { |b| POPCOUNT_TABLE[b] }
  string_bits: DATA.popcount
